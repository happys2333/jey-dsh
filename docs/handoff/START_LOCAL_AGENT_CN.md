# 复制给本地编码 Agent 的执行提示词

你要根据本目录的技术规格真正实现 **Agent Decision Layer（ADL）**，不是只评审文档或写概念方案。目标是 DSH 原生决策插件、共享核心、真实本地推理服务、官方 Jev 提供方、MCP 入口，以及完整测试、打包、CI 和授权上传流程。

先阅读本工作区原有 AGENTS.md（不要覆盖）及交接包 AGENTS.md，依次阅读 docs/01～05。执行 contracts/tasks.json 中的 M0～M8。

采用独立实现，不复制 buberlo/dsh-jev 的业务核心。先核实并固定 DeepSeek Harness 0.1.7-alpha.1 / c36a83ff6bb95e3f82cf79f9be7c724270a8aa61 的实际接口和安装行为，再实现公共契约。不得假造上游 API、通过禁用类型检查绕过不兼容，或在 pre-step 后盲目隐藏当前模型已经看到的工具。

先建立临时 DSH home 和测试工作区。完成核心纯策略，再跑真实 DSH + 可控测试模型的完整调用链；随后接入真实本地权重与云端协议。模型返回判断与宿主审批分开，不能放宽原权限。所有自动功能默认关闭，严格本地模式不回退云端，Mock 永远不能冒充真实本地推理。

持续完成实际代码、单测、属性测试、契约测试、真实宿主集成、取消/HMR/并发、安全测试、MCP 真进程、tarball 全新安装、真实推理和对照评测。实现文档规定的 pnpm adl:* 命令。每个 milestone 小提交并记录准确证据。发现规格与实际接口不符时，提交 ADR 并选最小兼容实现，不盲从错误接口。

不要因为普通实现选择停下来反复提问；按文档默认决定推进。缺少模型、GPU、云密钥、付费预算或远端授权时，只阻塞相关步骤，继续完成其他工程任务，绝不能把 SKIPPED/BLOCKED 记成 PASS。模型下载和付费测试遵守已给定授权；不得擅自消费或外传真实数据。

最后生成公共 staging 和 evidence.manifest.json，运行交接包 scripts/validate_evidence.py，打包源代码、安装包、报告与脱敏证据。若用户已明确提供并授权目标仓库/分支，按 docs/04 上传并核实远端 commit/CI；否则交付本地包并准确记录上传阻塞，不能猜仓库或自动公开发布。

最终回复必须包含：完整源 SHA、固定依赖、哪些测试真的运行、各 gate 状态、本地模型/硬件与离线证据、质量/性能结果、安装包/证据包路径与 SHA、已知问题、上传状态。不要只回复“完成”。
